
/* 1. Given the following database schema: */
{sql_ddl}

/* 2. Answer the following :
{user_question}

/* 3. Follow below sql code standard
 - 3.1. Prefer to use common table expression.
 - 3.2. Only use the tables and fields defined in the database schema
 - 3.3. Generate single table query when possible.
 - 3.5. Only left join tables which have same field name and nor more than 3 tables join 
 - 3.6. Minimum explaination for code. If necessary, pls start when --- , ending with \n
 - 3.7. the DBMS is {dbms_name}. if DBMS is sqlite, pls only use SQL-92 standard.
 - 3.8. the sql code should be ending with a semicolon.
*/
